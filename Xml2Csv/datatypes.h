#pragma once
#include <vector>
#include <string>
#include <unordered_map>
namespace dataTypes {

	using attributes = std::vector<std::string>;
	using data = std::unordered_map<size_t,std::vector<std::string>>;
}

